const Dialog = (
{children}) => (
    <div className="Dialog">
      <header>header</header>
      <main>main</main>
      <footer>footer</footer>
      {children}
    </div>
  );
  
  export default Dialog;
  