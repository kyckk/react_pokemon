const SideDialog = (
    {children}) => (
        <div className="Dialog Side">
          <header>header</header>
          <main>main</main>
          <footer>footer</footer>
          {children}
        </div>
      );
      
      export default SideDialog;
      